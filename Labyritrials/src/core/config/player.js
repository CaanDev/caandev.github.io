/**
 * @fileoverview Объект динамического состояния игрока.
 * Содержит все характеристики игрока: позицию, здоровье, оружие, эффекты и т.д.
 * 
 * @module core/config/player
 */

import { EMOJIS } from '../../emojis.js';

/**
 * @namespace player
 * @description Объект состояния игрока.
 * Все поля изменяются в процессе игры и сохраняются при сохранении.
 */
export const player = {
  // ============================================================
  // ПОЗИЦИЯ И ДВИЖЕНИЕ
  // ============================================================
  
  /**
   * @type {number} - Позиция по сетке X
   */
  x: 1,
  
  /**
   * @type {number} - Позиция по сетке Y
   */
  y: 1,
  
  /**
   * @type {number} - Позиция в пикселях X
   */
  px: 240,
  
  /**
   * @type {number} - Позиция в пикселях Y
   */
  py: 240,

  /** @type {boolean} - Движется ли игрок в текущем кадре */
  isMoving: false,
  
  // ============================================================
  // НАПРАВЛЕНИЕ АТАКИ
  // ============================================================
  
  /**
   * @type {number} - Цель атаки X (в пикселях)
   */
  targetX: 0,
  
  /**
   * @type {number} - Цель атаки Y (в пикселях)
   */
  targetY: 0,
  
  /**
   * @type {number} - Последнее направление движения X (для атаки)
   */
  lastMoveDirX: 0,
  
  /**
   * @type {number} - Последнее направление движения Y (для атаки)
   */
  lastMoveDirY: 1,

  /** @type {boolean} - Атака огненным шаром */
  isFireballAttack: false,
  
  // ============================================================
  // ХАРАКТЕРИСТИКИ
  // ============================================================
  
  /**
   * @type {number} - Текущее здоровье
   */
  hp: 100,
  
  /**
   * @type {number} - Максимальное здоровье
   */
  maxHp: 100,
  
  /**
   * @type {number} - Количество золота
   */
  gold: 0,
  
  /**
   * @type {number} - Количество собранных артефактов
   */
  artifactsCollected: 0,
  
  /**
   * @type {number} - Базовый урон
   */
  baseDamage: 20,
  
  /**
   * @type {number} - Множитель входящего урона
   */
  incomingDamageMultiplier: 1.0,
  
  // ============================================================
  // СКОРОСТЬ
  // ============================================================
  
  /**
   * @type {number} - Текущая скорость передвижения
   */
  speed: 6,
  
  /**
   * @type {number} - Базовая скорость передвижения
   */
  baseSpeed: 6,
  
  /**
   * @type {number|undefined} - Оригинальная скорость (для восстановления)
   */
  originalSpeed: undefined,
  
  /**
   * @type {number} - Таймер замедления (кадры)
   */
  slowTimer: 0,

  // ============================================================
  // ВЫНОСЛИВОСТЬ (STAMINA)
  // ============================================================
  
  /**
   * @type {number} - Текущая выносливость
   */
  stamina: 80,
  
  /**
   * @type {number} - Максимальная выносливость (стартовая)
   */
  maxStamina: 80,
  
  /**
   * @type {number} - Максимальный предел выносливости (после всех улучшений)
   */
  maxStaminaLimit: 150,
  
  /**
   * @type {number} - Восстановление выносливости в бою (в секунду)
   */
  staminaRegenInCombat: 6,
  
  /**
   * @type {number} - Восстановление выносливости вне боя (в секунду)
   */
  staminaRegenOutOfCombat: 10,
  
  /**
   * @type {number} - Время последней атаки по монстру (в миллисекундах)
   * Используется для определения статуса "в бою"
   */
  lastAttackTime: 0,
  
  /**
   * @type {number} - Стоимость улучшения выносливости в магазине
   */
  staminaUpgradeCost: 150,
  
  /**
   * @type {number} - Количество купленных улучшений выносливости
   */
  staminaUpgradeCount: 0,
  
  // ============================================================
  // ЭФФЕКТЫ
  // ============================================================
  
  /**
   * @type {number} - Таймер отравления (кадры)
   */
  poisonTimer: 0,
  
  /**
   * @type {number} - Тик отравления
   */
  poisonTick: 0,
  
  /**
   * @type {boolean} - Заморожен ли игрок
   */
  isFrozen: false,
  
  /**
   * @type {number} - Таймер заморозки (кадры)
   */
  freezeTimer: 0,
  
  /**
   * @type {number} - Таймер шока (кадры)
   */
  shockTimer: 0,
  
  /**
   * @type {number} - Замедление от шока (0-1)
   */
  shockSlowAmount: 0.6,
  
  // ============================================================
  // АТАКА
  // ============================================================
  
  /**
   * @type {boolean} - Идёт ли зарядка атаки
   */
  isCharging: false,
  
  /**
   * @type {number} - Время зарядки (кадры)
   */
  chargeTime: 0,
  
  /**
   * @type {boolean} - Активна ли атака
   */
  isAttacking: false,
  
  /**
   * @type {boolean} - Была ли выполнена атака
   */
  attackExecuted: false,
  
  /**
   * @type {number} - Таймер атаки (кадры)
   */
  attackTimer: 0,
  
  /**
   * @type {number} - Направление атаки X
   */
  dirX: 0,
  
  /**
   * @type {number} - Направление атаки Y
   */
  dirY: 1,
  
  // ============================================================
  // МАГАЗИН
  // ============================================================
  
  /**
   * @type {number} - Текущая стоимость улучшения HP
   */
  hpCost: 30,
  
  /**
   * @type {number} - Текущая стоимость улучшения урона
   */
  dmgCost: 40,
  
  /**
   * @type {boolean} - Есть ли карта лабиринта
   */
  hasMap: false,
  
  // ============================================================
  // УПРАВЛЕНИЕ
  // ============================================================
  
  /**
   * @type {boolean} - Инвертировано ли управление
   */
  controlsInverted: false,
  
  /**
   * @type {number} - Таймер инверсии (кадры)
   */
  invertTimer: 0,
  
  // ============================================================
  // СОБЫТИЯ
  // ============================================================
  
  /**
   * @type {number} - Множитель урона от событий
   */
  eventDamageMultiplier: 1.0,
  
  /**
   * @type {number} - Бонус скорости от событий
   */
  eventSpeedBonus: 0,
  
  /**
   * @type {number} - Множитель золота от событий
   */
  eventGoldMultiplier: 1.0,
  
  /**
   * @type {number} - Множитель замедления от событий
   */
  eventSlowMultiplier: 1.0,
  
  // ============================================================
  // БЛИЖНЕЕ ОРУЖИЕ
  // ============================================================
  
  /**
   * @type {string} - Текущее ближнее оружие ('default', 'stun', 'vampire')
   */
  meleeWeapon: 'default',
  
  /**
   * @type {string[]} - Купленное ближнее оружие
   */
  ownedMeleeWeapons: ['default'],
  
  // ============================================================
  // ДАЛЬНОБОЙНОЕ ОРУЖИЕ
  // ============================================================
  
  /**
   * @type {string|null} - Текущее дальнобойное оружие ('fireball' или null)
   */
  rangedWeapon: null,
  
  /**
   * @type {string[]} - Купленное дальнобойное оружие
   */
  ownedRangedWeapons: [],
  
  // ============================================================
  // ОГНЕННЫЙ ШАР
  // ============================================================
  
  /**
   * @type {number} - Текущий кулдаун огненного шара (кадры)
   */
  fireballCooldown: 0,
  
  /**
   * @type {number} - Максимальный кулдаун огненного шара (кадры)
   */
  fireballMaxCooldown: 180,
  
  // ============================================================
  // ВНЕШНИЙ ВИД
  // ============================================================
  
  /**
   * @type {string} - Эмодзи игрока
   */
  emoji: EMOJIS.player.default,
  
  // ============================================================
  // МНОЖИТЕЛИ
  // ============================================================
  
  /**
   * @type {number} - Множитель получаемого золота
   */
  goldMultiplier: 1.0,
  
  /**
   * @type {number} - Множитель вампиризма
   */
  vampMultiplier: 1.0,
  
  // ============================================================
  // ВАМПИРИЗМ
  // ============================================================
  
  /**
   * @type {number} - Процент лечения при обычном ударе (1% = 1)
   */
  vampireBasePercent: 1,
  
  /**
   * @type {number} - Процент лечения при сильном ударе (3% = 3)
   */
  vampireStrongPercent: 3,
  
  /**
   * @type {number} - Множитель лечения вампира (от алтаря)
   */
  vampireHealMultiplier: 1.0,
  
  /**
   * @type {number} - Бонус к лечению от артефактов (+0.5% за каждый)
   */
  vampireArtifactBonus: 0.5,
};